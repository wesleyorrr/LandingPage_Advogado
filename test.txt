/* Reset básico para remover margens e paddings padrão */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* Estilo geral do corpo */
body {
  font-family: Arial, sans-serif;
  line-height: 1.6;
  color: #333;
  overflow-x: hidden;
}

/* Estilo do cabeçalho principal */
header {
  position: relative;
  height: 100vh; /* Garante que o header ocupe toda a altura da tela */
  overflow: hidden; /* Impede que o conteúdo "vaze" para fora do header */
  background: url('../img/imgid.png') no-repeat center center;
  background-size: cover;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: center;
  color: #e0b063;
  text-align: center;
  padding: 2rem;
}

/* Estilo do conteúdo dentro do cabeçalho */
.header-content {
  max-width: 1000px; /* Limita a largura do conteúdo */
  margin: auto;
  text-align: center;
}

/* Estilo do título principal (h1) */
.header-content h1 {
  font-size: 3rem; /* Ajuste o tamanho conforme necessário */
  margin-bottom: 0.5rem;
  line-height: 1.2;
}

/* Estilo do subtítulo (h2) */
.header-content h2 {
  font-size: 1.5rem; /* Ajuste para manter o design consistente */
  margin-bottom: 2rem;
}

/* Estilo do botão no cabeçalho */
.header-content button {
  padding: 1rem 2rem;
  font-size: 1.2rem;
  border: none;
  background: #791c1c;
  color: white;
  cursor: pointer;
  border-radius: 8px;
  transition: transform 0.3s;
}

/* Efeito hover no botão */
.header-content button:hover {
  transform: scale(1.05);
}

/* Estilo da barra de navegação */
nav {
  width: 100%;
  position: absolute;
  top: 0;
  left: 0;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 50px;
  background: rgba(0, 0, 0, 0.4);
  z-index: 10;
}

/* Estilo do ícone do menu (para dispositivos móveis) */
.menu-icon {
  font-size: 2rem;
  color: #e0b063;
  cursor: pointer;
  display: none;
}

/* Estilo dos links de navegação (desktop) */
.nav-links {
  display: flex;
  gap: 1rem;
  align-items: center;
}

.nav-links a {
  color: white;
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s;
}

/* Efeito hover nos links de navegação */
.nav-links a:hover {
  color: #791c1c;
}

/* Estilo para dispositivos móveis */
@media (max-width: 768px) {
  /* Ajuste do título principal no mobile */
  .header-content h1 {
    font-size: 2rem; /* Tamanho menor para telas menores */
  }

  /* Exibe o ícone do menu no mobile */
  .menu-icon {
    display: block;
    position: absolute;
    right: 2rem;
    top: 1.5rem;
    z-index: 1000;
  }

  /* Estilo dos links de navegação no mobile */
  .nav-links {
    display: none;
    flex-direction: column;
    background-color: rgba(0, 0, 0, 0.95);
    position: absolute;
    top: 100%;
    left: 0;
    width: 100%;
    padding: 1rem 0;
    text-align: center;
    z-index: 1000;
  }

  /* Exibe os links de navegação quando o menu está ativo */
  .nav-links.show {
    display: flex;
  }

  /* Estilo dos links no mobile */
  .nav-links a {
    padding: 1rem 0;
    border-top: 1px solid #444;
  }

  /* Ajuste do container da navegação no mobile */
  nav.container {
    position: relative;
  }
}

/* Estilo do logotipo */
.logo {
  width: 50px;
  height: 50px;
}

/* Estilo adicional para o conteúdo do cabeçalho */
.header-content {
  margin-top: auto;
  margin-bottom: 4rem;
  max-width: 1000px;
}

/* Estilo do título principal (h1) no mobile */
.header-content h1 {
  font-size: 1.2rem;
  margin-bottom: 0.5rem;
  line-height: 1.2;
}

/* Estilo do subtítulo (h2) no mobile */
.header-content h2 {
  font-size: 1.2rem;
  margin-bottom: 2rem;
  font-weight: 400;
}

/* Estilo do botão no cabeçalho com animação */
.header-content button {
  padding: 1rem 2rem;
  font-size: 1.2rem;
  border: none;
  background: #791c1c;
  color: white;
  font-family: poppins, sans-serif;
  cursor: pointer;
  border-radius: 8px;
  animation: pulse 2.5s infinite ease-in-out;
  transition: transform 0.3s;
}

/* Animação de pulsar no botão */
@keyframes pulse {
  0% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.1);
  }
  100% {
    transform: scale(1);
  }
}

/* Estilo das seções gerais */
section {
  padding: 4rem 2rem;
}

/* Estilo das imagens dentro das seções */
section img {
  max-width: 50%;
  height: auto;
}

/* Estilo dos links sociais */
.social-links {
  display: flex;
  gap: 1rem;
  margin: 1rem 0;
}

.social-links a {
  color: #333;
  text-decoration: none;
  font-weight: bold;
  transition: color 0.3s;
}

/* Efeito hover nos links sociais */
.social-links a:hover {
  color: #0077b5;
}

/* Estilo do rodapé */
.footer {
  background-color: #111;
  color: #fff;
  padding: 2rem 1rem 1rem;
  margin-top: 3rem;
}

/* Estilo do conteúdo do rodapé */
.footer-content {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 2rem;
}

/* Estilo das colunas do rodapé */
.footer-column {
  flex: 1 1 250px;
  min-width: 250px;
}

/* Estilo dos títulos das colunas do rodapé */
.footer-column h3 {
  margin-bottom: 1rem;
  font-size: 1.4rem;
  color: #e0b063;
}

/* Estilo dos links e textos do rodapé */
.footer-column p,
.footer-column a {
  font-size: 1rem;
  color: #ddd;
  text-decoration: none;
  margin-bottom: 0.5rem;
}

/* Efeito hover nos links do rodapé */
.footer-column a:hover {
  color: #e0b063;
}

/* Estilo da parte inferior do rodapé */
.footer-bottom {
  text-align: center;
  margin-top: 2rem;
  padding-top: 1rem;
  border-top: 1px solid #444;
  font-size: 0.9rem;
  color: #aaa;
}

/* Estilo do mapa no rodapé */
.mapa iframe {
  border-radius: 8px;
  width: 100%;
  height: 180px;
}